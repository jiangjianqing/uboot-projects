2019.03.05
    *检查4412设备树文件，发现串口地址写的不正确，修正后重新尝试是否能进入uboot命令行
    希望网卡正常
    希望移植nand成功
    希望编译kernel并启动成功
    有篇

2019.03.04
    完成spl向uboot的跳转，成功进入uboot命令行
    print输出正常 （为啥spl部分的打印不正常？此问题还没有找到根源） 猜测：设备树正确配置了uboot串口驱动
    DM9000网卡无法ping通主机
    教训：别人写的资料上总是有一定错误，自己要仔细检查

2019.03.03
    在spl部分完成调试串口的字符输出，但遗留一个问题：前16个byte无法打印出来
    
